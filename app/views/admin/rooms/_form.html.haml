.form-horizontal
  = simple_form_for([:admin, @room], html: { :class => 'form-horizontal' }, wrapper: :bootstrap) do |f|

    .form-inputs
      = f.association :counselor
      = f.input :name

    .form-actions
      = f.button :submit, class: 'btn btn-success'

